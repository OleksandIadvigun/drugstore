variables:
  DOCKER_DRIVER: overlay2
  MAVEN_OPTS: "-Dhttps.protocols=TLSv1.2 -Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository -Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=WARN -Dorg.slf4j.simpleLogger.showDateTime=true -Djava.awt.headless=true"
  MAVEN_CLI_OPTS: "--batch-mode --errors --fail-at-end --show-version -DinstallAtEnd=true -DdeployAtEnd=true"
cache:
  untracked: true
  paths:
  - .m2/repository
test:
  stage: test
  image: maven:3.8.1-jdk-8-openj9
  services:
  - name: docker:dind
    alias: docker
  before_script:
  - docker pull rabbitmq:3.6.6-management-alpine
  - docker run --name rabbitmq --rm -d -p 5672:5672 rabbitmq:3.6.6-management-alpine
  script: SPRING_RABBITMQ_HOST=docker ./mvnw clean test
  after_script:
  - docker rm -f -v rabbitmq
